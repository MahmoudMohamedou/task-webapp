import { mdiArrowDown, mdiArrowUp, mdiCancel } from "@mdi/js";

export const icons: {
  [key: string]: string;
} = {
  mdiArrowUp: mdiArrowUp,
  mdiArrowDown: mdiArrowDown,
  mdiCancel: mdiCancel,
};
